# Props Definition Updates

* Add prop definition for `BaseInput` using a TypeScript interface
* Document required props for `ProjectColumnsTableInput`

## Slot Usage

* Replace incorrect slot syntax in `some-component.vue`
* Provide correct slot example using PrimeVue's `<template>` syntax