# PrimeVue Updates

## Component Naming

* Update `BaseButton` to use PascalCase (e.g., `BaseButton.vue`)

## Template Syntax

* Replace `<template>` with `<script setup>` in `some-component.vue`
* Correct template syntax for other components