# Root CI for the VibeTask monorepo (hub + frontend + app).
# Workflows under hub/.github, frontend/.github, and app/.github are legacy copies and are not used by GitHub.

name: VibeTask CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  hub:
    name: Hub (backend)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: hub
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: kanban_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: hub/package-lock.json
      - run: npm ci
      - name: Prisma validate & generate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/kanban_ci
        run: npx prisma validate && npx prisma generate
      - run: npm run lint:full
      - run: npm run build
      - run: npm run test
      - name: Integration tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/kanban_ci
          BETTER_AUTH_SECRET: ci-vibetask-better-auth-secret-32chars-min
          BETTER_AUTH_URL: http://localhost:3000
          PLATFORM_SESSION_SECRET: ci-vibetask-platform-session-secret-32
          NODE_ENV: test
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-client
          npx prisma migrate deploy
          npm run db:seed
          npm run test:integration

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: OpenAPI matches hub
        run: npm run openapi:check-sync
      - run: npm run lint:style
      - name: Unit tests
        env:
          VITE_API_BASE_URL: http://localhost:3000
          VITE_WS_BASE_URL: http://localhost:8080
        run: npm run test:unit
      - name: Build
        env:
          VITE_API_BASE_URL: http://localhost:3000
        run: npm run build

  app:
    name: App (Rust)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            app/target
          key: ${{ runner.os }}-cargo-app-${{ hashFiles('app/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-app-
      - run: cargo fmt --all --check
      - run: cargo clippy --all-targets --all-features -- -D warnings
      - run: cargo test --all-features
